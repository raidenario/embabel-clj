# Build output — unanchored, so nested projects (examples/, experiments/) are covered too
target/

# Clojure
.cpcache/
.nrepl-port

# Tooling caches — `**/` is required: a pattern with an inner slash is anchored to root
**/.clj-kondo/.cache/
.lsp/
.calva/

# IntelliJ / IDE
/.idea/
*.iml

# Logs
*.log

# Secrets / env
.env
*.env

# OS
.DS_Store
Thumbs.db
